# Server Configuration
PORT=8000
HOST=0.0.0.0

# Uvicorn Worker Configuration
# Number of worker processes (default: 2x CPU cores)
# For Zeabur deployment:
# - Developer Plan (2 vCPU): recommended 4 workers
# - Team Plan (4 vCPU): recommended 8 workers
# - Enterprise: adjust based on allocated vCPUs
UVICORN_WORKERS=8

# Gemini Vision API Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# FFmpeg Configuration (for frame extraction)
TEMP_DIR=/tmp/recipeai
MAX_VIDEO_SIZE_MB=500

# Cloudflare R2 Configuration (for thumbnail storage)
R2_ACCOUNT_ID=your_r2_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=recipeai-thumbnails
R2_PUBLIC_URL=https://thumbnails.recipeai.com

# Instagram Cookies
# Instagram authentication cookies are automatically loaded from R2:
# https://pub-69fc9d7b005d450285cb0cee6d8c0dd5.r2.dev/thumbnails/www.instagram.com_cookies.txt
#
# To update cookies (when expired after 1-3 months):
# 1. Extract cookies from Chrome: yt-dlp --cookies-from-browser chrome --cookies instagram_cookies.txt "https://www.instagram.com/"
# 2. Upload to R2: python3 upload_cookies.py
# 3. No service restart needed - changes take effect immediately
#
# IMPORTANT:
# - Use a test Instagram account, not your personal account
# - Update every 1-3 months when cookies expire
# - The R2 file is publicly accessible but contains session-specific cookies

# Observability
LOG_LEVEL=INFO

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
